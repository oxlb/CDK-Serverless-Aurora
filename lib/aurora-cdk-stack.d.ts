import { Construct, Stack, StackProps } from '@aws-cdk/core';
export declare class AuroraSlsStack extends Stack {
    constructor(scope: Construct, id: string, props: StackProps, deploymentStage: string);
}
